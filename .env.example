# Contact form endpoint — no EmailJS configuration needed.
# The form now posts directly to /api/contact.php (PHP mailer on Hostinger/cPanel).
# No environment variables required for local development.
