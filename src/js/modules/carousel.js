// carousel.js is now empty / minimal — the marquee has been removed
// The partner grid is CSS-only, no JS needed
// Keep the file but export an empty init:
export function initCarousel() {
    // Scroll is CSS-only. No JS needed.
}
